services:
  honcho-mission-control:
    build: .
    ports: ["3000:3000"]
    env_file:
      - path: ./runtime/dashboard.env
        required: false
    volumes:
      - "${HERMES_KANBAN_SNAPSHOT_HOST_DB:-./runtime/kanban.db}:/data/hermes/kanban.db:ro"
    restart: unless-stopped
